// Footer removed per request. This component is now a no-op so existing
// <Footer /> usages elsewhere continue to compile without rendering anything.
// If you'd like, you can also remove the <Footer /> import/usage from your
// layout file entirely.
const Footer = () => {
  return null;
};

export default Footer;